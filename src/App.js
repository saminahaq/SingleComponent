import './App.css'
import SubComponent from './components/SubComponent'
function App(){
    return (<div>
        <SubComponent />
        </div>
    );
}

export default App
//here we need to put the default , because if we changed the name on index.js
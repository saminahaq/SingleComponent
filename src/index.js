import  ReactDOM  from "react-dom";
import './index.css'
import App from  './App.js';

//ReactDOM.render(<h2>Samina</h2>, document.getElementById('root'));
ReactDOM.render(<App />, document.getElementById('root'))

//so now we need to pass the componenet that has two step 
//1: capitalized first later
//2: under the tag  < />